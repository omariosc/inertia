using inertia.Enums;

namespace inertia.Authorization;

/// <summary>
/// List of roles
/// </summary>
public class Roles
{
    public const string User = nameof(AccountRole.User);
    public const string Employee = nameof(AccountRole.Employee);
}