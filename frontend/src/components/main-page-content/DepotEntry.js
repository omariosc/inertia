export default function DepotEntry() {
    /*
       TODO: implement this. this shows the details of a depo (maybe with a book now button that redirects
       redirects to the booking menu
    */

    return (
        <div>

        </div>
    )
}