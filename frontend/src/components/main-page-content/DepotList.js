export default function DepotList() {
    /*
       TODO:
       - if it's called from '/', display the normal list
       - if it's called from '/depots?search=Word', filter through the list
    */

    return (
        <div>

        </div>
    )
}